# STYLE-SELF D-1 案A(タブなし完全チャット)/ 案B(最小ナビ)比較設計案

作成日: 2026-05-20
位置づけ: 本ドキュメントは **比較設計"案"**。本体設計書
          `docs/STYLE-SELF_D1_実装設計.md`(現 HEAD `6917bb8`・チャット主役型)は
          **書き換えない**。オーナー合意後に必要なら本体へ反映する議論材料。

背景: P1-B(BottomNav 5→3 集約)実機 FB で
      「3 タブのままだとまだ『タブある普通アプリ』に見える」と判明。
      ChatGPT の服版 = チャットがアプリそのもの、を真に実現するには
      **下部ナビ自体を撤去** すべきかを比較検討する。

> ★ 最重要判断軸:
> 「ユーザーは機能を探しているか? それとも自然に話しかけているか?」
> 後者(自然に話しかける)に近い方を優先評価する。

---

## 1. 現状(実物・P1-A / P1-B 後)

```
commit 状態(2026-05-20 時点):
  c77a54c feat(P1-A) 起動導線変更(/ai 新規 + redirect + middleware)
  79a76be feat(P1-B) BottomNav 5→3 集約(AI / 保存 / 自分)

実物資産:
  ・/ai = 最小骨格(P1-C で ChatPage 実装予定)
  ・components/overlay/OverlayFab.tsx(右下 FAB・D1-2b' のモーダル起動)
  ・components/overlay/OverlayModal.tsx(対話モーダル・履歴 state + 吹き出し + 5 サブ)
  ・lib/overlay/navigate-map.ts(意図 → URL 表・9 entry + タブ命名トリック吸収)
  ・app/api/overlay/intent/route.ts(D1-1 意図判定 API・Haiku ¥0.001/件)
  ・既存 18 機能 API(M1-M5・全完成・全温存)
  ・app/(public)/u/[id] / p/[id](★ SNS シェア互換 絶対不変)
  ・/saved(3 セクション)/ /self(5 サブタブ:diagnosis 世界観 / posts / body / worldview 好み / history)
  ・/onboarding(16 問・BottomNav 非表示)
  ・/home /discover /outfit + 旧 shim 9 個(P1-B で BottomNav から外したが URL は残置)

navigate-map.ts の 9 entry(チャットコマンドで再利用可能):
  diagnose          → /onboarding
  worldview-profile → /self?tab=diagnosis(タブ命名トリック吸収)
  create-post       → /self/new-post
  my-posts          → /self?tab=posts
  closet            → /outfit?tab=closet
  saved             → /saved
  history           → /self?tab=history
  body-edit         → /self?tab=body
  preference-edit   → /self?tab=worldview(同上)
```

→ タブなしにしても **既存 18 機能・公開ページ・/onboarding は URL レベルで全部生きている**。
   到達手段(チャットコマンド・右上メニュー)を新層として被せるだけで成立する基盤がある。

---

## 2. 案A / 案B フラット比較表(8 観点 + 最重要判断軸)

| # | 観点 | 案A: タブなし完全チャット | 案B: 最小ナビ(3 タブ・P1-B 現状) | 優位 |
|---|---|---|---|---|
| 1 | ユーザーが機能を探さずに使えるか | 機能名はどこにも見えない・全部「話しかける」| 「保存」「自分」が常時下部に見え押せる(=探せる)| **案A** |
| 2 | 初見で直感的に使えるか | 最初は「何ができるか」が分からない・提案チップ 5 で誘導必須 | 3 タブで「何があるか」が一目で分かる | **案B** |
| 3 | ChatGPT の服版に見えるか | 完全に ChatGPT 型(タブなし・チャットがアプリ)| ChatGPT を起動するボタンを置いた「普通アプリ」 | **案A** |
| 4 | 保存・自分・クローゼットを前面に出さなくても成立するか | 右上メニュー + チャットコマンドで到達可・前面非表示が本質 | 下部に常時出ているため前面に出さない選択肢がない | **案A** |
| 5 | 既存 18 機能を裏側で温存できるか | navigate-map + intent ルーターで全到達可・既存 API 不変 | 同上(両案で温存可)| **同等** |
| 6 | M3 投稿 / SNS 公開 /u/[id] /p/[id] を壊さず残せるか | 公開ページは独立(public ルートグループ)で BottomNav 非表示=元から無関係・絶対不変 | 同上(両案で不変)| **同等** |
| 7 | 将来リアル試着・購入導線を足しやすいか | 「着せて」→ 試着・「これ買う」→ 商品 が全てチャット内で完結 | 試着・購入もチャット経由可だが、ナビとの役割分担が曖昧化する | **案A**(やや)|
| 8 | 「何を押すか」でなく「何を言うか」と思えるか | タブなしは強い意思表示・押すものが入力欄しかない | タブが視野にあると「押そう」と無意識に探す | **案A** |

#### 集計

```
案A優位: 5 観点(1, 3, 4, 7, 8)
案B優位: 1 観点(2 = 初見直感性)
同等  : 2 観点(5, 6 = 既存温存)
```

### ★ 最重要判断軸(自然に話しかける体験)に照らした総合評価

```
「ユーザーは機能を探しているか? 自然に話しかけているか?」

案A: ★ 強く後者に振っている
  - 下部ナビが視野に無い = 押すものがない = 入力欄に話しかけるしかない
  - 機能名は一切前面に出ない(右上メニューは「設定」相当・常時 push しない)

案B: 中間
  - チャットがメインだが、下部 3 タブが常に視野にあり「保存タブを押そう」が誘発される
  - 自然文を書く前にタブを押してしまう可能性が残る
```

→ 最重要判断軸に照らせば **案A が明確に優位**。
  案B が勝つ唯一の観点(初見直感性)は、提案チップ 5・初回チュートリアル吹き出し 等で
  カバー可能(後述 3.1)。

---

## 3. 案A の具体設計(オーナーが傾いている案)

### 3.1 起動画面イメージ(`/ai` ChatPage・P1-C で構築)

```
┌─────────────────────────────────────────────┐
│ STYLE-SELF AI     [黒い美術館の住人]    [≡] │ ← 上部(常時表示)
│                   ↑世界観カード(小)    ↑右上メニュー
├─────────────────────────────────────────────┤
│                                               │
│  初回時のみ:                                  │
│  「今日は何を相談しますか?」                 │
│  ┌──────────────┐ ┌──────────────┐         │
│  │💡 コーデ提案 │ │🔍 似た世界観 │         │ ← 提案チップ 5 種
│  └──────────────┘ └──────────────┘         │   (補助・初回 onboarding 的)
│  ┌──────────────┐ ┌──────────────┐         │
│  │💬 着こなし相談│ │📚 ブランド学ぶ│         │
│  └──────────────┘ ┌──────────────┐         │
│  ┌──────────────┐ │🎨 ムードボード│         │
│  │👗 試着(Phase3)│└──────────────┘         │
│  └──────────────┘                           │
│                                               │
│  [既往の対話履歴がここに積まれる]            │ ← 中央チャット履歴(スクロール)
│                                               │
├─────────────────────────────────────────────┤
│ [📎写真] [🔗商品URL] [👕クローゼット] [🎨MB] │ ← 入力欄近くのアクションボタン
│ ┌────────────────────────────────────┐ [送]│
│ │ 自然文で書いてください…              │     │
│ └────────────────────────────────────┘     │
└─────────────────────────────────────────────┘
※ 下部ナビなし(BottomNav 撤去 = 案A の核)
```

### 3.2 ★ 右上メニュー設計([≡] アイコンタップで展開)

```
[≡] →
┌──────────────────────┐
│ あなたの世界観        │ → /self?tab=diagnosis
│ クローゼット          │ → /outfit?tab=closet
│ 保存                  │ → /saved
│ 身体・好み・避けたい  │ → /self?tab=body 等
│ 履歴                  │ → /self?tab=history
│ 投稿                  │ → /self?tab=posts(M3 自分の投稿)
│ ──────────────       │
│ 設定                  │ → 将来
│ ログアウト            │ → /auth/signout(既存)
└──────────────────────┘
```

→ 前面に主張しない・「いざという時の手段」として残す。
   毎回使う機能(コーデ提案・相談・マッチ)はチャットで完結するため、右上メニューは
   実質「設定とプロフィール管理」のための入口。

### 3.3 ★ チャットコマンド設計(navigate-map 転用で実現)

`/api/overlay/intent`(D1-1)+ navigate-map(D1-2a)を **そのまま活用**:

| ユーザー発話例 | intent | 結果 |
|---|---|---|
| 「保存見せて」「保存リスト」| `saved` | 「保存リストを開きますね →」+ /saved 引き継ぎ |
| 「俺の世界観見せて」 | `worldview-profile` | 同上 + /self?tab=diagnosis |
| 「体型変更したい」「身長変えたい」| `body-edit` | + /self?tab=body |
| 「クローゼットに追加したい」「服を登録」| `closet` + ボタン押下で /outfit?tab=closet |
| 「投稿作りたい」 | `create-post` | + /self/new-post |
| 「自分の投稿見せて」 | `my-posts` | + /self?tab=posts |
| 「再診断したい」 | `diagnose` | + /onboarding |
| 「履歴見せて」 | `history` | + /self?tab=history |
| 「好みを編集したい」 | `preference-edit` | + /self?tab=worldview |

→ ★ **既存 D1-1 intent ルーター + D1-2a navigate-map で全 9 件の専用 UI 引き継ぎは実現可能**。
   新規実装ゼロでチャットコマンドが成立する。

### 3.4 ★ 既存資産の到達手段マップ(★ 迷子ゼロ確認)

| 既存資産 | 案A での到達手段 |
|---|---|
| **対話完結 8 機能** | チャット内で完結(coordinate / style-consult / inspiration / match-users / match-posts / culture / brand-learn / virtual-coordinate)|
| **専用 UI 引き継ぎ 10 機能** | チャットコマンド or 提案チップ → navigate-map 経由で各画面へ |
| **M3 投稿作成**(/self/new-post) | チャットコマンド「投稿作りたい」or 右上メニュー「投稿」|
| **M3 自分の投稿管理**(/self?tab=posts) | 右上メニュー「投稿」or チャット「自分の投稿」|
| **M2-3 公開プロフ /u/[id]** | ★ **絶対不変**(SNS シェア互換・URL は外部から到達)|
| **M3-4 投稿個別公開 /p/[id]** | ★ **絶対不変**(同上)|
| **保存 /saved** | 右上メニュー「保存」or チャット「保存見せて」|
| **クローゼット /outfit?tab=closet** | 右上メニュー「クローゼット」or 入力欄「クローゼット参照」ボタン or チャット「クローゼット見せて」|
| **身体・好み・避けたい /self?tab=body / =worldview / avoid_items** | 右上メニュー or チャット「体型変更」「好み編集」|
| **AI 履歴 /self?tab=history** | 右上メニュー or チャット「履歴見せて」|
| **再診断 /onboarding** | チャット「再診断したい」or 右上メニュー(将来)|
| **発見 /discover の各サブタブ** | 各 intent から到達(inspiration/learn/culture/worldview-match は対話完結)|
| **コーデ /outfit の各サブタブ** | 同上(coordinate/consult/virtual は対話完結・closet は引き継ぎ)|
| **/home(旧ホーム)** | 案A では到達手段なし(機能は他で全 cover・URL 直アクセスのみ生存)|

→ ★ **到達できない機能ゼロ**。/home は機能セットの集約画面で、内容は全て他で cover。

### 3.5 下部ナビなしで onboarding / 公開ページ / 旧 URL がどうなるか

| ルート | 案A での挙動 |
|---|---|
| `/onboarding` | 既存通り全画面(BottomNav 非表示・元から)|
| `/u/[id]` `/p/[id]` | 既存通り(public ルートグループで BottomNav 元から非表示)|
| `/self` `/saved` `/outfit` 等 | URL 直アクセスは生存・画面は表示される・右上メニュー or 戻るボタンで /ai へ |
| `/home` `/discover` 等 | URL 直アクセスは生存(P1-B 判断 2 で残置)・将来 redirect to /ai 検討 |

---

## 4. 案B の具体設計(P1-B 現状)

### 4.1 構造(P1-B 現状)

```
┌─────────────────────────────────────────────┐
│ STYLE-SELF AI     [黒い美術館の住人]         │
├─────────────────────────────────────────────┤
│  チャット履歴                                  │
│  ...                                          │
├─────────────────────────────────────────────┤
│ 入力欄 + 送信                                 │
├─────────────────────────────────────────────┤
│ [✨ AI] [🔖 保存] [👤 自分]                  │ ← 下部ナビ 3 タブ(P1-B)
└─────────────────────────────────────────────┘
```

### 4.2 案A との体験差

```
- 下部ナビが常時視野 → 「保存タブを押せばいい」と無意識に学習する
- チャットを使う前に「タブで探す」モードに入ってしまう可能性
- ChatGPT の体験(タブなし)とは距離がある

ただし:
- 初見ユーザーが「ここから機能にアクセスできる」と理解しやすい
- 保存・自分 が常時 1 タップで到達できる(チャットコマンドより速い)
```

### 4.3 案A よりも軽い変更

```
P1-A / P1-B 完了の現状から最小調整(右上メニュー・チャットコマンドの実装不要)。
P1-C 以降は ChatPage 構築のみで MVP 到達。
```

---

## 5. ★ 既存資産の温存(両案共通・絶対不変)

### 5.1 不可侵 8 項目(設計書 4.4)— 両案で完全保持

```
1. 既存 18 機能のコード・API・DB に一切触れない
2. M2-3 SELECT 列絞り / M4-2 public_users view / M5 coreTags 並列 を迂回しない
3. 既存 API 入出力契約を変更しない
4. ★ /u/[id] /p/[id] 公開ページ URL 構造を絶対不変
5. 旧画面ファイル(/home /discover /outfit)を Phase 1 で削除しない
6. worldview_tags 英語スラッグを UI に露出しない
7. service_role を使わない
8. ③ プライバシー専章(本体設計書 6 章)/ コスト管理(7 章)/ Phase 2 後ゲート 完全不変
```

= **変わるのは「ユーザーの到達導線」だけ。機能・API・データ・プライバシー設計は両案で一切変えない**。

### 5.2 SNS 公開ページ /u/[id] /p/[id] の絶対不変

```
両案とも以下を厳守:
- app/(public)/u/[userId]/page.tsx
- app/(public)/p/[postId]/page.tsx
これらは BottomNav 非表示の独立ルートグループ・外部 SNS シェア URL として使われる。
案A でタブを撤去しても、本ルートは元から BottomNav と無関係 = 影響ゼロ。
```

---

## 6. 機能優先順位の整理(オーナー指定)

### 6.1 MVP 必要(言う → AI がやる を強くする)

| # | 機能 | 案A での見え方 | 案B での見え方 |
|---|---|---|---|
| ① | AI チャット | メイン画面そのもの | メイン画面 + 下部 AI タブ(冗長)|
| ② | 世界観プロフィール(AI 裏参照)| 上部世界観カード(小)+ AI が常時参照 | 同上 |
| ③ | 身体 / 好み / 避けたい印象 | チャット内で AI が参照 + 編集はチャット/右上メニュー | 同上 + 自分タブから編集 |
| ④ | クローゼット / 欲しい服 | 裏データ(AI が参照)+ 入力欄ボタンで明示参照 | 同上(自分タブの先) |
| ⑤ | 保存 | 右上メニュー / チャット「保存見せて」 | 下部保存タブ |
| ⑥ | 画像 / 商品 URL / ムードボードをチャットに渡す | 入力欄近くの 4 ボタン([📎写真][🔗URL][👕クローゼット][🎨MB])| 同上(タブと並立) |

### 6.2 後回し(前面に出さない・将来)

| 機能 | 案A での扱い | 案B での扱い |
|---|---|---|
| 発見 / コミュニティ / 投稿(他人)| 提案チップ「似た世界観」のみ・URL 残置 | 同上(発見タブが消えた現状)|
| カルチャー一覧 | チャット「カルチャー」で intent 発火・カード返却 | 同上 |
| 複雑な履歴 | 右上メニュー / チャット | 自分タブの先 |
| コーデ専用タブ / 診断専用タブ | チャットコマンド or 提案チップ | 同上 |

→ **「後回し」= 機能削除でなく前面に出さないだけ**。M3 投稿/既存機能のコード・API・SNS 公開は両案で温存。チャット/メニューから到達可能。

---

## 7. 移行の安全性(MVPとして)

### 7.1 案A 採用時の段階分割(P1-B 現状から)

```
案A-α: BottomNav 撤去 + 右上メニュー追加(layout.tsx 改修 + 新規 MenuDrawer コンポーネント)
案A-β: ChatPage 構築(D1-2b' 履歴 state / 5 サブ転用・OverlayFab 廃止 = 現 P1-C)
案A-γ: 提案チップ 5 種・上部世界観カード・入力欄アクション 4 ボタン(現 P1-D 相当)
案A-δ: 対話完結 8 結果カード(現 P1-E 相当・プライバシー漏洩点検)
案A-ε: virtual→product 連鎖 + 次アクション 3(現 P1-F 相当)
案A-ζ: 仕上げ + 退行点検 + 知見追記(現 P1-G 相当)
```

→ 既存 P1-C〜G に **「α: ナビ撤去 + 右上メニュー」が 1 段追加** されるだけ。
   αは小さい改修(BottomNav.tsx の return null + layout.tsx に Drawer 追加)で済む。

### 7.2 案B 採用時(P1-C 以降の現行ステップ)

```
P1-C ChatPage 構築 → P1-D 〜 P1-G(本体設計書 6917bb8 のまま)
```

→ 既に確定したルートを進むだけ。地雷小・予測可能。

### 7.3 既存破壊リスク

| リスク | 案A | 案B |
|---|---|---|
| BottomNav 撤去で既存タブ訪問者が迷う | 中(/home /discover /outfit が URL 残置・但したどり着く手段は外部リンクのみ)| 低(3 タブ常時表示)|
| 右上メニューに機能を集約 → メニュー実装の地雷 | 中(新規コンポーネント・テスト要)| なし |
| チャットコマンドが正しく意図判定するか | 中(navigate-map で 9 件カバー・テスト要)| 同上(案B でもチャット入口は同じ)|
| SNS 公開 /u/[id] /p/[id] への影響 | ゼロ(独立ルートグループ)| ゼロ |
| M3 投稿 / EXIF / 画像処理への影響 | ゼロ(/self/new-post 独立)| ゼロ |
| ③ プライバシー / コスト / ゲート | ゼロ(両案で本体不変)| ゼロ |

→ 案A の追加リスクは「ナビ撤去 + 右上メニュー実装」の 1 段だけ。
   既存資産破壊リスクは両案ともゼロ(本体不可侵境界線維持)。

---

## 8. 設計者推奨

### 8.1 評価サマリ

| 評価軸 | 案A | 案B |
|---|---|---|
| 最重要判断軸(自然に話しかける体験)| ★ **強く優位** | 中間 |
| 8 観点優位 | **5 観点** | 1 観点 |
| 既存資産温存 | ★ 同等(両案で不可侵保持)| ★ 同等 |
| MVP 安全性(既存破壊リスク)| 追加リスク 小(α 1 段のみ)| 最小(P1-B のまま継続)|
| 実装コスト | + 1 段(α: ナビ撤去 + 右上メニュー)| 現行ステップで完結 |
| ChatGPT の服版感 | ★ **完全に達成** | 「ChatGPT 起動ボタン付き普通アプリ」感残る |

### 8.2 推奨: ★ **案A(タブなし完全チャット型)**

理由:
1. ★ 最重要判断軸「自然に話しかける体験」で **強く優位**(5 観点)
2. オーナーの「ChatGPT の服版」志向と完全に整合(8 観点 3 / 7 / 8)
3. 既存資産温存は両案で同等(本体不可侵境界線維持)
4. 既存破壊リスクは「α: ナビ撤去 + 右上メニュー」の 1 段追加のみで、SNS 公開ページ /u /p
   や M3 投稿等への影響はゼロ
5. 案A の唯一の弱点(初見直感性)は提案チップ 5 種・初回 onboarding 的吹き出しで cover 可能
6. navigate-map 9 件 + intent ルーターを **そのまま転用** すれば、チャットコマンドで
   全機能到達可能 = 新規実装ゼロで成立する基盤がある

### 8.3 反対意見・懸念(検討材料として記録)

```
案B 支持の論拠:
- 初見ユーザーが「ここに何があるか」が分からない = 学習コスト
- 「保存」「自分」を 1 タップで開ける速さ
- 案A 採用すると P1-C 前に α(ナビ撤去 + Drawer)を挟む必要があり、工程が 1 段増える

案A 支持の論拠が上回ると判断する理由:
- 学習コスト = 提案チップ 5 種で初回誘導すれば実質ゼロ
- 1 タップの速さ = 「保存」「自分」は毎日使うものでなく週 1 回程度 → 多少遅くても許容
- 工程 1 段増 = α は最小規模(layout.tsx 改修 + Drawer 新規)で地雷小
```

### 8.4 最終決定はオーナー

```
本ドキュメントは判断材料。
最終決定はオーナーが実機での体験ビジョン(ChatGPT の服版をどこまで純化するか)で行う。

設計者推奨は ★ 案A だが、案B(P1-B 現状継続)でも MVP は成立する。
オーナーが「P1-B のままで P1-C 以降を進めるか」「α を挟んで案A に切り替えるか」を
本ドキュメントを参照しつつ決める。
```

---

## 9. このドキュメントの位置づけ

```
docs/STYLE-SELF_ビジョン統合マップ.md(最上位)
  ├ ... M1〜M5 設計
  ├ STYLE-SELF_D1_実装設計.md(本体・6917bb8・チャット主役型・P1-B まで反映済)
  ├ STYLE-SELF_D1_対話中心_改訂案.md(対話中心改訂の経緯記録・統合済)
  ├ STYLE-SELF_D1_チャット主役型_設計案.md(チャット主役型化の経緯記録・統合済)
  └ STYLE-SELF_D1_案A案B比較.md(このファイル・P1-B 後の純化判断材料)

合意が取れたら:
  - 案A 採用 → 本体設計書を「タブなし完全チャット型」に再々改訂 + P1-A〜G を α-ζ に再構成
  - 案B 継続 → 本体設計書はそのまま・P1-C 以降を順次実装
```
